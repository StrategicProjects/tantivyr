% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writer.R
\name{tnt_delete}
\alias{tnt_delete}
\title{Delete documents by field value}
\usage{
tnt_delete(idx, condition)
}
\arguments{
\item{idx}{A \code{tnt_index}.}

\item{condition}{An expression of the form \code{field == value}. \code{value} may be a
vector, deleting all matching documents.}
}
\value{
The \code{tnt_index}, invisibly.
}
\description{
Marks for deletion every document whose \code{field} equals the given value(s),
using a \code{field == value} expression. Deletions take effect after
\code{\link[=tnt_commit]{tnt_commit()}}. For reliable deletion the field should be an exact field: a
numeric/date field, or a text field created with \code{stemmer = "raw"}.
}
\examples{
sch <- tnt_schema(id = tnt_i64(), body = tnt_text(stemmer = "english"))
idx <- tnt_index(schema = sch)
tnt_add(idx, data.frame(id = 1:3, body = c("a", "b", "c"))) |> tnt_commit()
tnt_delete(idx, id == 2) |> tnt_commit()
tnt_num_docs(idx)
}
\seealso{
\code{\link[=tnt_update]{tnt_update()}}
}
