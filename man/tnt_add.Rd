% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writer.R
\name{tnt_add}
\alias{tnt_add}
\title{Add documents to an index}
\usage{
tnt_add(idx, data)
}
\arguments{
\item{idx}{A \code{tnt_index}.}

\item{data}{A data frame whose columns map to schema fields by name.}
}
\value{
The \code{tnt_index}, invisibly (so calls can be piped).
}
\description{
Adds the rows of \code{data} as documents. Only columns whose names match a schema
field are used; other columns are ignored. Additions become searchable after
\code{\link[=tnt_commit]{tnt_commit()}}.
}
\examples{
sch <- tnt_schema(id = tnt_i64(), body = tnt_text(stemmer = "english"))
idx <- tnt_index(schema = sch)
df <- data.frame(id = 1:2, body = c("the quick fox", "lazy dogs sleep"))
idx |> tnt_add(df) |> tnt_commit()
tnt_num_docs(idx)
}
\seealso{
\code{\link[=tnt_commit]{tnt_commit()}}, \code{\link[=tnt_update]{tnt_update()}}
}
