% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schema.R
\name{tnt_field}
\alias{tnt_field}
\alias{tnt_text}
\alias{tnt_i64}
\alias{tnt_u64}
\alias{tnt_f64}
\alias{tnt_bool}
\alias{tnt_date}
\alias{tnt_json}
\title{Define schema fields}
\usage{
tnt_text(
  stored = TRUE,
  indexed = TRUE,
  fast = FALSE,
  stemmer = "none",
  stopwords = FALSE
)

tnt_i64(stored = TRUE, indexed = TRUE, fast = FALSE)

tnt_u64(stored = TRUE, indexed = TRUE, fast = FALSE)

tnt_f64(stored = TRUE, indexed = TRUE, fast = FALSE)

tnt_bool(stored = TRUE, indexed = TRUE, fast = FALSE)

tnt_date(stored = TRUE, indexed = TRUE, fast = FALSE)

tnt_json(stored = TRUE, indexed = TRUE)
}
\arguments{
\item{stored}{Logical. Keep the original value so it is returned by
\code{\link[=tnt_search]{tnt_search()}}. Defaults to \code{TRUE}.}

\item{indexed}{Logical. Index the field so it can be searched or filtered.
Defaults to \code{TRUE}.}

\item{fast}{Logical. Build a columnar "fast field" enabling fast filtering
and ordering (required by \code{order_by} in \code{\link[=tnt_search]{tnt_search()}}). Defaults to
\code{FALSE}, except \code{tnt_text(fast = ...)} which has no fast field by default.}

\item{stemmer}{Stemming/tokenization for a text field. One of \code{"none"} (plain
tokenization, lower-cased), \code{"raw"} (exact, untokenized — ideal for ids and
exact filters), or a Snowball language such as \code{"portuguese"} or
\code{"english"}. See \code{\link[=tnt_stemmers]{tnt_stemmers()}}.}

\item{stopwords}{Logical. Remove stop words for the chosen language. Bundled
for Portuguese and English. Defaults to \code{FALSE}.}
}
\value{
A \code{tnt_field} object.
}
\description{
These constructors describe a single field in a \code{\link[=tnt_schema]{tnt_schema()}}. Each returns
a lightweight \code{tnt_field} object.
}
\examples{
tnt_schema(
  id    = tnt_i64(),
  title = tnt_text(stemmer = "portuguese", stopwords = TRUE),
  body  = tnt_text(stemmer = "portuguese"),
  date  = tnt_date()
)
}
\seealso{
\code{\link[=tnt_schema]{tnt_schema()}}
}
