% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writer.R
\name{tnt_update}
\alias{tnt_update}
\title{Update documents (delete then re-add)}
\usage{
tnt_update(idx, data, by)
}
\arguments{
\item{idx}{A \code{tnt_index}.}

\item{data}{A data frame of replacement documents.}

\item{by}{<\code{\link[tidyselect:language]{tidy-select}}> A single key column present
in both \code{data} and the schema.}
}
\value{
The \code{tnt_index}, invisibly.
}
\description{
Replaces documents by deleting any existing document that shares a key value
(the \code{by} field) with a row of \code{data}, then adding the rows of \code{data}. The
\code{by} field must be an exact field (numeric/date, or text with
\code{stemmer = "raw"}). Call \code{\link[=tnt_commit]{tnt_commit()}} afterwards.
}
\examples{
sch <- tnt_schema(id = tnt_i64(), body = tnt_text(stemmer = "english"))
idx <- tnt_index(schema = sch)
tnt_add(idx, data.frame(id = 1:2, body = c("old one", "old two"))) |>
  tnt_commit()
tnt_update(idx, data.frame(id = 1L, body = "new one"), by = id) |>
  tnt_commit()
}
\seealso{
\code{\link[=tnt_add]{tnt_add()}}, \code{\link[=tnt_delete]{tnt_delete()}}
}
