% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{tnt_index_df}
\alias{tnt_index_df}
\title{Index a data frame in one call}
\usage{
tnt_index_df(
  data,
  text,
  filters = NULL,
  stemmer = "none",
  stopwords = FALSE,
  stored = TRUE,
  path = NULL,
  overwrite = FALSE,
  heap_mb = 128
)
}
\arguments{
\item{data}{A data frame.}

\item{text}{<\code{\link[tidyselect:language]{tidy-select}}> Columns to index as
full-text fields.}

\item{filters}{<\code{\link[tidyselect:language]{tidy-select}}> Columns to index for
filtering/ordering (their type is inferred). Optional.}

\item{stemmer, stopwords}{Stemming and stop-word options applied to all \code{text}
columns. See \code{\link[=tnt_text]{tnt_text()}}.}

\item{stored}{Logical. Store text columns so they are returned by searches.}

\item{path, overwrite, heap_mb}{Passed to \code{\link[=tnt_index]{tnt_index()}}.}
}
\value{
A committed \code{tnt_index} object.
}
\description{
A convenience wrapper that infers a schema from \code{data}, creates an index,
adds every row and commits. Text columns are made searchable; filter columns
are indexed for filtering and ordering; all other columns are stored so they
are returned by \code{\link[=tnt_search]{tnt_search()}}.
}
\examples{
df <- data.frame(
  id = 1:2,
  title = c("Orçamento público aprovado", "Reforma tributária avança"),
  year = c(2023L, 2024L)
)
idx <- tnt_index_df(df, text = title, filters = year, stemmer = "portuguese")
tnt_search(idx, "orcamento")
}
\seealso{
\code{\link[=tnt_index]{tnt_index()}}, \code{\link[=tnt_search]{tnt_search()}}
}
