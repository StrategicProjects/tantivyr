% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{tnt_index}
\alias{tnt_index}
\title{Create or open a search index}
\usage{
tnt_index(path = NULL, schema = NULL, overwrite = FALSE, heap_mb = 128)
}
\arguments{
\item{path}{Directory for an on-disk index, or \code{NULL} for an in-memory index.
If the directory already contains an index it is opened (unless
\code{overwrite = TRUE}).}

\item{schema}{A \code{\link[=tnt_schema]{tnt_schema()}}. Required when creating a new index; ignored
when opening an existing one.}

\item{overwrite}{Logical. If \code{TRUE}, an existing index at \code{path} is deleted
and recreated from \code{schema}. Defaults to \code{FALSE}.}

\item{heap_mb}{Indexing memory budget in MB (minimum 15). Defaults to 128.}
}
\value{
A \code{tnt_index} object.
}
\description{
Opens an existing on-disk index, or creates a new one from a \code{\link[=tnt_schema]{tnt_schema()}}.
With \code{path = NULL} an in-memory index is created (useful for tests and
transient work).
}
\examples{
sch <- tnt_schema(
  id    = tnt_i64(),
  title = tnt_text(stemmer = "english"),
  body  = tnt_text(stemmer = "english")
)
idx <- tnt_index(schema = sch) # in-memory
idx
}
\seealso{
\code{\link[=tnt_index_df]{tnt_index_df()}}, \code{\link[=tnt_add]{tnt_add()}}, \code{\link[=tnt_search]{tnt_search()}}
}
