% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{tnt_search}
\alias{tnt_search}
\title{Search an index}
\usage{
tnt_search(
  idx,
  query = "",
  limit = 10L,
  fields = NULL,
  filter = NULL,
  highlight = NULL,
  snippet_chars = 150L,
  order_by = NULL,
  desc = TRUE
)
}
\arguments{
\item{idx}{A \code{tnt_index}.}

\item{query}{A query string in tantivy's
\href{https://docs.rs/tantivy/latest/tantivy/query/struct.QueryParser.html}{query syntax}.
The empty string \code{""} matches all documents (useful with \code{filter}).}

\item{limit}{Maximum number of results. Defaults to 10.}

\item{fields}{<\code{\link[tidyselect:language]{tidy-select}}> Text fields searched for
bare (unqualified) query terms. Defaults to all indexed text fields.}

\item{filter}{Either a tantivy query string, or a comparison expression such
as \code{year >= 2020 & source == "globo"}. Supported operators: \code{==}, \code{\%in\%},
\code{>}, \code{>=}, \code{<}, \code{<=}, combined with \code{&} and \code{|}.}

\item{highlight}{<\code{\link[tidyselect:language]{tidy-select}}> Stored text fields to
return highlighted snippets for. One \verb{<field>_snippet} column is added per
selected field, with matches wrapped in \verb{<b>} tags.}

\item{snippet_chars}{Maximum snippet length in characters. Defaults to 150.}

\item{order_by}{<\code{\link[tidyselect:language]{tidy-select}}> A single numeric/date
\emph{fast} field to order by instead of BM25 score. When set, \code{score} is \code{NA}.}

\item{desc}{Logical. Order descending (the default) when \code{order_by} is set.}
}
\value{
A tibble with a \code{score} column, every stored field, and any requested
\verb{*_snippet} columns.
}
\description{
Runs a BM25 full-text search and returns the top matches as a tibble. Supports
structured filters, result ordering and snippet highlighting.
}
\examples{
df <- data.frame(
  id = 1:3,
  title = c("Quick brown fox", "Lazy dog", "Brown bear"),
  year = c(2019L, 2021L, 2023L)
)
idx <- tnt_index_df(df, text = title, filters = year, stemmer = "english")
tnt_search(idx, "brown")
tnt_search(idx, "brown", filter = year >= 2021)
tnt_search(idx, "fox", highlight = title)
}
\seealso{
\code{\link[=tnt_count]{tnt_count()}}, \code{\link[=tnt_index_df]{tnt_index_df()}}
}
