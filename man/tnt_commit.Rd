% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/writer.R
\name{tnt_commit}
\alias{tnt_commit}
\title{Commit pending changes}
\usage{
tnt_commit(idx)
}
\arguments{
\item{idx}{A \code{tnt_index}.}
}
\value{
The \code{tnt_index}, invisibly.
}
\description{
Flushes buffered additions and deletions to the index and refreshes the
reader so they become visible to \code{\link[=tnt_search]{tnt_search()}}.
}
\examples{
sch <- tnt_schema(body = tnt_text(stemmer = "english"))
idx <- tnt_index(schema = sch)
tnt_add(idx, data.frame(body = "hello")) |> tnt_commit()
}
